<%= form_for(@delivery_exception) do |f| %>
  <% if @delivery_exception.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@delivery_exception.errors.count, "error") %> prohibited this delivery_exception from being saved:</h2>

      <ul>
      <% @delivery_exception.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exceptionType %><br />
    <%= f.text_field :exceptionType %>
  </div>
  <div class="field">
    <%= f.label :exceptionDescription %><br />
    <%= f.text_area :exceptionDescription %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
