<h1>Listing employee_manager_assignments</h1>

<table>
  <tr>
    <th>Startdate</th>
    <th>Enddate</th>
    <th>Activeboolean</th>
    <th>Employee</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employee_manager_assignments.each do |employee_manager_assignment| %>
  <tr>
    <td><%= employee_manager_assignment.startDate %></td>
    <td><%= employee_manager_assignment.endDate %></td>
    <td><%= employee_manager_assignment.activeBoolean %></td>
    <td><%= employee_manager_assignment.employee %></td>
    <td><%= link_to 'Show', employee_manager_assignment %></td>
    <td><%= link_to 'Edit', edit_employee_manager_assignment_path(employee_manager_assignment) %></td>
    <td><%= link_to 'Destroy', employee_manager_assignment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employee manager assignment', new_employee_manager_assignment_path %>
