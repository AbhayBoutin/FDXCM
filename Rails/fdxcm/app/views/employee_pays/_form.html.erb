<%= form_for(@employee_pay) do |f| %>
  <% if @employee_pay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_pay.errors.count, "error") %> prohibited this employee_pay from being saved:</h2>

      <ul>
      <% @employee_pay.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee %><br />
    <%= f.text_field :employee %>
  </div>
  <div class="field">
    <%= f.label :startDate %><br />
    <%= f.datetime_select :startDate %>
  </div>
  <div class="field">
    <%= f.label :endDate %><br />
    <%= f.datetime_select :endDate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
