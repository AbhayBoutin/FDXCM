psazip.rb to psa_zip.rb

remove state class generation:
 `psa: belongs_to`
 and put in the POST section
 	has_many:psaStates
 and change in the POST section
 	 `has_many:psaStates, :through => psaStates` to `has_many:states, :through => psaStates `

remove from ZIP `psa:belongs_to` from gerenation and place in post `	psa:belongs_to`


MIGRATIONS
remove