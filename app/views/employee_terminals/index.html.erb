<h1>Listing employee_terminals</h1>

<table>
  <tr>
    <th>Startdate</th>
    <th>Enddate</th>
    <th>Activeboolean</th>
    <th>Employee</th>
    <th>Terminal</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employee_terminals.each do |employee_terminal| %>
  <tr>
    <td><%= employee_terminal.startDate %></td>
    <td><%= employee_terminal.endDate %></td>
    <td><%= employee_terminal.activeBoolean %></td>
    <td><%= employee_terminal.employee %></td>
    <td><%= employee_terminal.terminal %></td>
    <td><%= link_to 'Show', employee_terminal %></td>
    <td><%= link_to 'Edit', edit_employee_terminal_path(employee_terminal) %></td>
    <td><%= link_to 'Destroy', employee_terminal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employee terminal', new_employee_terminal_path %>
