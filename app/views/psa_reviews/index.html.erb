<h1>Listing psa_reviews</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @psa_reviews.each do |psa_review| %>
  <tr>
    <td><%= link_to 'Show', psa_review %></td>
    <td><%= link_to 'Edit', edit_psa_review_path(psa_review) %></td>
    <td><%= link_to 'Destroy', psa_review, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Psa review', new_psa_review_path %>
